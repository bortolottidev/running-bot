name: Scheduled Job Actions Check 

on:
  push:
    branches:
      - master 
  schedule:
    - cron: '* * * * *'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Run the check on deployed server
        run: echo "Hello Scheduled"
